services:
  web:
    build: .
    image: worldmapguessr:latest
    container_name: worldmapguessr
    restart: unless-stopped
    ports:
      - "${WMG_PORT:-5000}:5000"
    volumes:
      - wmg-instance:/app/instance

volumes:
  wmg-instance:
